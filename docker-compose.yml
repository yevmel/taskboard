services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: taskboard
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "25432:5432"
    volumes:
      - taskboard-pg:/var/lib/postgresql/

volumes:
  taskboard-pg:
